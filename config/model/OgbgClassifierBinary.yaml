model_name: GraphClassifier
resume_training_from_ckpt:
  path:
pl_config:
  requires_dense: True
  type: BINARY
  batch_size: 64
  lr: 1e-3
  model_config:
    num_classes: 2
    num_layers: 3
    filter_channels: 20
    ff_channels: 201
