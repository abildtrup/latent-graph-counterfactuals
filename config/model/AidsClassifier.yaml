model_name: GraphClassifier
resume_training_from_ckpt:
  path:
pl_config:
  requires_dense: False
  type: SPARSE
  batch_size: 64
  lr: 1e-4
  model_config:
    num_features: 9
    num_classes: 2
    num_layers: 3
    dim: 20
    dropout: 0.0
